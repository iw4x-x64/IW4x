include <cstdint>; // std::uint64_t

namespace iw4x
{
  class options
  {
    bool --help
    {
      "Print usage information and exit."
    };

    bool --version
    {
      "Print version and exit."
    };

    bool --cpptrace-no-colors
    {
      "Disable colors in stack traces."
    };

    bool --cpptrace-no-snippets
    {
      "Disable code snippets in stack traces."
    };

    bool --cpptrace-no-filter
    {
      "Disable filtering in stack trace."
    };
  };
}
